# 协作开发指南

## 📁 文件命名规范

### 目录命名
- 使用 **kebab-case**（小写 + 连字符）
- 示例：`seat-management/`, `data-card/`

### 文件命名
- HTML：`kebab-case.html`（如 `seat-list.html`）
- CSS：`kebab-case.css`（如 `data-card.css`）
- JS：`camelCase.js`（如 `seatManager.js`）

## 🎨 CSS 规范

### BEM 命名
```css
/* Block */
.sidebar {}

/* Element */
.sidebar__nav {}
.sidebar__nav-item {}

/* Modifier */
.sidebar--collapsed {}
.sidebar__nav-item--active {}
```

### 样式文件结构
```css
/**
 * 组件名称
 * 用途说明
 */

/* ========== 变量引用 ========== */
/* 使用 /core/styles/variables.css 中定义的变量 */

/* ========== 基础样式 ========== */
.component {}

/* ========== 元素样式 ========== */
.component__element {}

/* ========== 修饰符 ========== */
.component--modifier {}

/* ========== 响应式 ========== */
@media (max-width: 1200px) {}
```

## 📦 组件开发规范

### 组件目录结构
```
/core/components/[component-name]/
├── index.html          # 组件 HTML 结构
├── style.css           # 组件样式
├── script.js           # 组件逻辑（可选）
└── README.md           # 组件文档
```

### 组件 README 模板
```markdown
# 组件名称

## 用途
简述组件功能

## 使用方式
```html
<!-- 示例代码 -->
```

## 属性/配置
| 属性 | 类型 | 默认值 | 说明 |
|-----|------|-------|-----|

## 变体
- 变体1：说明
- 变体2：说明
```

## 📄 模块开发规范

### 模块目录结构
```
/modules/[module-name]/
├── README.md           # 模块说明（必须）
├── /pages/             # 页面文件
│   └── [page].html
├── /components/        # 模块私有组件
│   └── [component]/
└── /styles/            # 模块样式
    └── [page].css
```

### 模块 README 模板
```markdown
# 模块名称

## 功能描述
模块的业务功能说明

## 页面列表
| 页面 | 文件 | 说明 |
|-----|------|-----|

## 依赖组件
- /core/components/xxx

## 数据结构
```json
{
  "字段": "说明"
}
```

## 交互说明
1. 用户操作 → 系统响应
```

## 🔄 Git 工作流

### 分支命名
- 功能：`feature/[module]-[feature]`
- 修复：`fix/[module]-[issue]`
- 示例：`feature/dashboard-chart`, `fix/settings-form`

### Commit 规范
```
[type]([scope]): [description]

type: feat | fix | style | refactor | docs
scope: 模块名或组件名
```

## ✅ 代码审查清单

- [ ] 遵循 BEM 命名规范
- [ ] 使用 variables.css 中的变量
- [ ] 组件有 README 文档
- [ ] 无硬编码的颜色/尺寸值
- [ ] 响应式适配（如需要）
- [ ] 无 console.log 遗留
