# StudySpace 管理员桌面端前端架构

> 基于 Axure 低保真原型生成的可协作、AI 友好的前端项目结构

## 📋 项目概述

本项目是 StudySpace 学习空间管理系统的**管理员桌面端**前端实现，采用纯 HTML/CSS/JS 技术栈，支持多人并行开发和 AI 辅助生成。

### 源页面映射

| Axure 原型页面 | 模块 | 目标页面 |
|---------------|------|---------|
| 桌面管理员端-数据概览.html | dashboard | /modules/dashboard/pages/overview.html (预约管理) |
| 桌面管理员端-座位管理.html | seat-management | /modules/seat-management/pages/seat-list.html |
| 桌面管理员端-设置.html | settings | /modules/settings/pages/index.html |

### 侧边栏菜单（完整）
| 菜单项 | 模块目录 | 说明 |
|-------|---------|------|
| 预约管理 | /modules/dashboard/ | 座位预约管理面板 |
| 任务 | /modules/task/ | 任务管理 |
| 日历 | /modules/calendar/ | 日历视图 |
| 学习记录 | /modules/study-record/ | 学习记录查看 |
| 座位管理 | /modules/seat-management/ | 座位状态管理 |
| 管理员设置 | /modules/settings/ | 系统设置 |

## 🏗️ 项目结构

```
admin-desktop/
├── index.html                    # 应用入口
├── README.md                     # 项目说明（本文件）
├── CONTRIBUTING.md               # 协作开发指南
│
├── /core/                        # 全局核心资源
│   ├── /components/              # 通用组件库
│   │   ├── sidebar/              # 侧边栏导航
│   │   ├── navbar/               # 顶部导航栏
│   │   ├── search-bar/           # 搜索栏
│   │   ├── filter-dropdown/      # 筛选下拉框
│   │   ├── data-card/            # 数据统计卡片
│   │   ├── table/                # 数据表格
│   │   ├── modal/                # 弹窗组件
│   │   ├── button/               # 按钮组件
│   │   ├── form-controls/        # 表单控件
│   │   └── icons/                # 图标库
│   │
│   ├── /layouts/                 # 页面布局模板
│   │   └── admin-layout/         # 管理员端主布局
│   │
│   ├── /styles/                  # 全局样式
│   │   ├── variables.css         # CSS 变量（色板、间距、字体）
│   │   ├── reset.css             # 样式重置
│   │   ├── typography.css        # 排版规范
│   │   ├── utilities.css         # 工具类
│   │   └── main.css              # 样式入口
│   │
│   └── /utils/                   # 工具函数
│       ├── dom.js                # DOM 操作
│       ├── api.js                # API 请求封装
│       ├── storage.js            # 本地存储
│       └── helpers.js            # 通用辅助函数
│
├── /modules/                     # 业务模块（按功能拆分）
│   ├── /dashboard/               # 预约管理模块
│   ├── /task/                    # 任务模块
│   ├── /calendar/                # 日历模块
│   ├── /study-record/            # 学习记录模块
│   ├── /seat-management/         # 座位管理模块
│   └── /settings/                # 设置模块
│
└── /assets/                      # 静态资源
    ├── /images/                  # 图片资源
    ├── /fonts/                   # 字体文件
    └── /icons/                   # 图标文件
```

## 🎨 设计规范

详见 `/core/styles/variables.css` 中的完整定义。

### 色板
- 主色：`#1890FF`（品牌蓝）
- 成功：`#52C41A`
- 警告：`#FAAD14`
- 错误：`#FF4D4F`
- 文字：`#333333`（主）/ `#666666`（次）/ `#999999`（辅助）
- 背景：`#FFFFFF`（主）/ `#F5F5F5`（次）/ `#FAFAFA`（卡片）

### 字体
- 主字体：`Inter, -apple-system, BlinkMacSystemFont, sans-serif`
- 标题：32px / 24px / 18px / 14px / 13px / 10px
- 正文：14px（行高 1.5）

### 间距系统（8px 基准）
- xs: 4px | sm: 8px | md: 16px | lg: 24px | xl: 32px | xxl: 48px

### BEM 命名规范
```css
.block {}
.block__element {}
.block--modifier {}
```

## 🚀 快速开始

1. 克隆项目后，直接用浏览器打开 `index.html`
2. 开发时建议使用 Live Server 等本地服务器
3. 各模块可独立开发，最终通过主入口整合

## 🤖 AI 协作指南

本项目结构专为 AI 辅助开发优化：

1. **模块独立性**：每个 `/modules/xxx/` 目录可独立交给 AI 生成
2. **README 驱动**：每个模块都有 README.md 描述功能需求
3. **组件复用**：AI 生成页面时应优先使用 `/core/components/` 中的组件
4. **样式一致性**：所有样式必须基于 `variables.css` 中的变量

### AI Prompt 模板
```
请基于以下信息为 [模块名] 生成代码：
- 参考：/modules/[模块名]/README.md
- 使用组件：/core/components/
- 遵循样式：/core/styles/variables.css
- 输出到：/modules/[模块名]/pages/[页面名].html
```

## 📝 开发规范

详见 `CONTRIBUTING.md`
